# scCausal replication environment
# Python >= 3.9

numpy>=1.24.0
scipy>=1.10.0
pandas>=2.0.0
matplotlib>=3.7.0
scienceplots>=2.1.0
networkx>=3.0
scanpy>=1.9.0
statsmodels>=0.14.0
scikit-learn>=1.3.0
torch>=2.0.0
tqdm>=4.65.0
requests>=2.28.0
h5py>=3.8.0
# Only needed to render the PNG previews of the TikZ figures (1 and 3);
# the manuscript itself embeds the vector PDFs.
PyMuPDF>=1.23.0
